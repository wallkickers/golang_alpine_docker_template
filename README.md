# golang_alpine_docker_template
GoのDocker実行環境。OSはAlpine。

```
git clone https://github.com/wallkickers/golang_alpine_docker_template.git
cd golang_alpine_docker_template
cd env
docker-compose up -d
docker-compose exec app ash
```
